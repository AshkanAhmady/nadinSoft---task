import { Box } from '@mui/material';

const TodosPage = () => {
    return (
        <Box
            sx={{
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
                gap: "1rem",
                flexDirection: 'column',
                minHeight: '100%',
            }}
        >todos page</Box>
    );
}

export default TodosPage;