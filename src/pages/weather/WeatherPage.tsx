import { Box } from '@mui/material';

const WeatherPage = () => {
    return (
        <Box
            sx={{
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
                gap: "1rem",
                flexDirection: 'column',
                minHeight: '100%',
            }}
        >weather page</Box>
    );
}

export default WeatherPage;